extends ./includes/layout
block body
    h1 Internal Server Error
        h2 Something isn't right here.
        if (typeof errors !== 'undefined')
            pre.error
                code
                    - _.each(errors, function (error) {
                    = error
                    - });
        else
            p.error
                | A team of highly trained non professionals is working on this as we speak.
                br
                | If the problem persists, please
                a(href="mailto://admin@erlystage.com") contact the system administrator
                |  and inform them of the time that the error occured, and anything you might have done that may have caused the error.
                .logo
        a(href="/")
            | Go to Home Page